#!/bin/sh
# postinst script for ChineseChess
#
# see: dh_installdeb(1)

set -e

# summary of how this script can be called:
#        * <postinst> `configure' <most-recently-configured-version>
#        * <old-postinst> `abort-upgrade' <new version>
#        * <conflictor's-postinst> `abort-remove' `in-favour' <package>
#          <new-version>
#        * <postinst> `abort-remove'
#        * <deconfigured's-postinst> `abort-deconfigure' `in-favour'
#          <failed-install-package> <version> `removing'
#          <conflicting-package> <version>
# for details, see https://www.debian.org/doc/debian-policy/ or
# the debian-policy package

INSTALL_ROOT=/opt/ChineseChess
case "$1" in
    configure)
	    mkdir -p $INSTALL_ROOT/etc
        chmod -R a+rw $INSTALL_ROOT/log

        if [ ! -f /usr/share/applications/org.Rabbit.ChineseChess.desktop ]; then
            ln -s /opt/ChineseChess/share/applications/org.Rabbit.ChineseChess.desktop /usr/share/applications/org.Rabbit.ChineseChess.desktop
        fi
        if [ ! -f /usr/share/pixmaps/org.Rabbit.ChineseChess.png ]; then
            if [ ! -d /usr/share/pixmaps ]; then
                mkdir -p /usr/share/pixmaps
            fi
            ln -s /opt/ChineseChess/share/pixmaps/org.Rabbit.ChineseChess.png /usr/share/pixmaps/org.Rabbit.ChineseChess.png
        fi
        export QT_VERSION_DIR=
        if [ -n "${QT_VERSION_DIR}" ]; then
            echo "/opt/qt${QT_VERSION_DIR}/lib" >> /etc/ld.so.conf.d/ChineseChess.conf
        fi
        ldconfig
    ;;

    abort-upgrade|abort-remove|abort-deconfigure)
        rm -fr /usr/share/applications/org.Rabbit.ChineseChess.desktop
        rm -fr /etc/ld.so.conf.d/ChineseChess.conf
        rm -fr /usr/share/pixmaps/org.Rabbit.ChineseChess.png
    ;;

    *)
        echo "postinst called with unknown argument \`$1'" >&2
        exit 1
    ;;
esac

# dh_installdeb will replace this with shell code automatically
# generated by other debhelper scripts.

#DEBHELPER#

exit 0
